# 性能基准测试方案

## 指标
- 首屏渲染时间（Home、Posts 列表）
- 交互延迟（搜索框输入防抖后过滤耗时）
- 无限滚动加载耗时与吞吐

## 方法
- 使用 Lighthouse 本地跑分；记录改动前后分数。
- 使用 Web Vitals（FCP、LCP、CLS、INP）；在开发环境注入采样。
- 使用简单脚本统计 API 响应与渲染时序。

## 交付
- 保存两次跑分截图与数值，PR 中对比。

